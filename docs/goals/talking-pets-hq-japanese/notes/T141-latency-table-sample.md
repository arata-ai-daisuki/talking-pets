# T141 latency table sample

## 結論

Platform verification issueへ貼るlatency tableの形が分かるように、実機検証docsへプレースホルダー表を追加した。

## 変更

- `npm run latency:table` で生成したMarkdown表をIssueへ貼るときの列構成サンプルを追加した。
- サンプル値はbenchmark dataではなくplaceholderであることを明記した。
- 生成WAV添付や新しい実測claimは増やしていない。

## 状態

active。
