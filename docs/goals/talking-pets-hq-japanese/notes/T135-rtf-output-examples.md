# T135 RTF output examples

## 結論

PR #41で追加した `audioDuration` / `rtf` が、協力者向けの測定手順から分かるように出力例を追加した。

## 変更

- `docs/real-device-verification.md` のVOICEVOX Latency Contributionへ、RTF付き `[latency]` 行の例を追加した。
- `docs/performance.md` のVOICEVOX / Irodori helper欄へ、`audioDuration` / `rtf` を含む例を追加した。
- 生成WAVは添付しないよう明記した。

## 判断

READMEには追加しない。初見向けREADMEを重くせず、測定の詳細はdocsへ集約する。

## 状態

active。
